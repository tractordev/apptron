[package]
name = "hostbridge"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
libc = "0.2.2"
wry = { version = "0.13.1", default-features = false, features = ["tray"] }
notify-rust = "4.5.6"
